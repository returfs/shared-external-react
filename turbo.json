{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "rollup": {
      "outputs": ["dist/**"]
    },
    "rollup:watch": {
      "persistent": true,
      "outputs": ["dist/**"]
    }
  }
}
